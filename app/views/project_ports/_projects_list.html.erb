<%# raise %>
<div class="projects-list">
  <% projects.each do |project| %>
    <div class="project">
      <h2><%= project.name %></h2>
      <p><%= project.description %></p>
      <p><%= Department.find_by(id: project.department_id).name %></p>
    </div>
  <% end %>
</div>
